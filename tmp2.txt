363:     gemba_sel = st.dataframe(        df_view,        use_container_width=True,        hide_index=True,        column_config={
464:             )            st.dataframe(                dg_df,                use_container_width=True,                hide_index=True,                column_config={
588:                 )                st.dataframe(                    fechas_df,                    use_container_width=True,                    hide_index=True,                    column_config={
635:                         st.dataframe(                df_prod_display[cols_prod].sort_values(by=[c for c in ["Correlativo"] if c in cols_prod], ascending=True),                use_container_width=True,                hide_index=True,                height=300,
666:             st.info("Selecciona una fila en la tabla GEMBA para habilitar el detalle.")    with col_det_2:        abrir = st.button("📄 Ver", use_container_width=True, disabled=not bool(codigo_sel))    if abrir and codigo_sel:
742:                 cols_u = [c for c in ["CodigoLicitacion", "Nombre", "C_Usuario", "FechaCierre", "Dias_a_Cierre"] if c in urg.columns]                st.dataframe(                    urg[cols_u].head(20),                    use_container_width=True,                    hide_index=True,                    column_config={
767:                     sin_adj = sin_adj.sort_values("FechaCierre", ascending=False)                st.dataframe(                    sin_adj[cols_s].head(15),                    use_container_width=True,                    hide_index=True,                    column_config={
789:         )        top_usr["MontoEstimado"] = top_usr["MontoEstimado"].map("${:,.0f}".format)        st.dataframe(top_usr, use_container_width=True, hide_index=True)    st.markdown("---")
855:                     by=[c for c in ["FechaAdjudicacion"] if c in df_adj_view.columns],                    ascending=False,                ).head(50),                use_container_width=True,                hide_index=True,                column_config={
884:                     )                    st.dataframe(                        resumen,                        use_container_width=True,                        hide_index=True,                        column_config={
918:                     by=[c for c in ["FechaCierre", "FechaPublicacion"] if c in df_des_view.columns],                    ascending=False,                ).head(50),                use_container_width=True,                hide_index=True,                column_config={
2226:     col_btn1, col_btn2 = st.columns(2)    with col_btn1:        if st.button("📨 Enviar a Compradores", use_container_width=True, type="primary"):            # Obtener credenciales de la sesión si están configuradas            email_usar = st.session_state.get('email_config', OUTLOOK_EMAIL)
2243:                 # Mostrar resultados detallados                if resultados:                    df_resultados = pd.DataFrame(resultados)                    st.dataframe(df_resultados, use_container_width=True, hide_index=True)                                        # Mostrar ayuda adicional si hay errores de autenticación
2265:                             """)    with col_btn2:        if st.button("📊 Enviar a Jefaturas", use_container_width=True, type="primary"):            # Obtener credenciales de la sesión si están configuradas            email_usar = st.session_state.get('email_config', OUTLOOK_EMAIL)
2282:                 # Mostrar resultados detallados                if resultados:                    df_resultados = pd.DataFrame(resultados)                    st.dataframe(df_resultados, use_container_width=True, hide_index=True)                                        # Mostrar ayuda adicional si hay errores de autenticación
2478:             showlegend=True,            legend=dict(orientation="v", yanchor="middle", y=0.5, x=1.15)        )        st.plotly_chart(fig_torta, use_container_width=True)        with col_graf2:
2516:                 hovertemplate='<b>%{y}</b><br>Cantidad: %{x}<br>Carga Ponderada: %{customdata[0]:.0f}<br>Monto: $%{customdata[1]:,.0f}<extra></extra>',                customdata=conteo_tipo[["CargaPonderada", "MontoTotal"]].values            )            st.plotly_chart(fig_barras, use_container_width=True)        else:            st.info("No hay información de Tipo disponible para este comprador.")
2554:                 st.dataframe(            resumen_display,            use_container_width=True,            hide_index=True,            column_config={
2615:             hovertemplate='<b>%{y}</b><br>Carga Ponderada: %{x:.0f}<br>Licitaciones: %{customdata[0]}<br>En Proceso: %{customdata[1]}<br>Monto: $%{customdata[2]:,.0f}<extra></extra>',            customdata=carga_por_comprador[["CantidadLicitaciones", "EnProceso", "MontoTotal"]].head(15).values        )        st.plotly_chart(fig_carga, use_container_width=True)                # Tabla detallada
2631:                 st.dataframe(            carga_display,            use_container_width=True,            hide_index=True,            column_config={
2669:                 by=[c for c in ["EstadoClasificado", "FechaClave"] if c in df_detalle.columns],                ascending=[True, True]            ),            use_container_width=True,            hide_index=True,            column_config={